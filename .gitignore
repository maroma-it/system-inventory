# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.agents/
.claude/
*.swp

# Excel lock files
~$*

# Optional: ignore regenerated outputs if you only want source in the repo.
# Leave commented if you want output/ tracked so others can view without rebuilding.
# output/

# Sensitive working files
*.private.json
scratch/

# Local reporting working folder. Holds DevExpress .rxml layouts (which embed the
# production connection string), customer-data Excel exports, and a duplicate of a
# data/ workspace export. docs/ is the GitHub Pages publish target, so nothing in
# here may be tracked. Findings live in reviews/rxml-corpus-audit-*.md instead.
docs/reporting/

# Per-machine launcher preference (start.bat last-opened view)
output/last-view.txt
output/snapshots/.snapshot.lock
